<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>franka_rt_controllers</name>
  <version>0.1.0</version>
  <description>
    Real-time ros2_control velocity blending controller for Franka FR3.
    Performs tracking/avoidance blending, rate limiting, smooth timeout ramp,
    and final velocity clamp inside the 1 kHz RT loop – eliminating
    sample-and-hold jitter from non-RT Python pipelines.
  </description>
  <maintainer email="mattia@todo.todo">mattia</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>

  <depend>controller_interface</depend>
  <depend>hardware_interface</depend>
  <depend>pluginlib</depend>
  <depend>rclcpp</depend>
  <depend>rclcpp_lifecycle</depend>
  <depend>realtime_tools</depend>
  <depend>std_msgs</depend>
  <depend>sensor_msgs</depend>
  <depend>franka_msgs</depend>
  <depend>ament_index_cpp</depend>
  <depend>pinocchio</depend>
  <depend>franka_semantic_components</depend>
  <depend>eigen3_cmake_module</depend>
  <depend>Eigen3</depend>

  <exec_depend>launch</exec_depend>
  <exec_depend>launch_ros</exec_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
