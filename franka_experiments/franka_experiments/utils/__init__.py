"""Utility modules for franka_experiments.

After refactoring:
  - ``selfcheck.py`` merged into ``ros.py`` → :func:`ros.selfcheck_run`
  - ``joint_state.py`` merged into ``kinematics.py`` → :class:`kinematics.JointStateManager`
"""
