topics:
  joint_states_topic: /NS_1/joint_states
  velocity_topic: /NS_1/avoidance_qdot

params:
  d_safe: 0.3
  rho_slack: 1000.0
  qp_solver: osqp
  ema_alpha: 0.4
  # qdot_min / qdot_max removed — FR3 per-joint limits are hardcoded in the node