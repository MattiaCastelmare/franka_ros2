[develop]
script_dir=$base/lib/franka_experiments
[install]
install_scripts=$base/lib/franka_experiments
[tool:pytest]
# test/launch contains manual *.launch.py files (not pytest tests); their
# test_* names would otherwise break pytest collection with import errors.
norecursedirs = launch
